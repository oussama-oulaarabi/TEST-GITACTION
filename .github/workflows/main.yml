name: myworkfloaw
on:
 push:
   branches: [main/* , main ,master/**]
   branches-ignore:
     - master1
   paths:
     - "release/**/*"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: echo "hello oussama this work "
      
    
