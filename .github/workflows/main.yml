name: myworkfloaw
on:
 push:
   branches: [main/* , main ,master/**]
   paths:
     - "release/**/*"
     - "test1.txt"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: echo "hello oussama this work "
      
    
