name: myworkfloaw
on:
  cron: '*/2 * * * *'
   
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: echo "hello oussama this work "
        run: echo {{toJson(github.event.schedule)}}
      - name: step2
        run: echo "${{toJson(github.actor)}}"
      
    
