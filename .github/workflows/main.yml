name: myworkflow

on:
  schedule:
    - cron: '*/5 * * * *' 
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: |
          echo "hello oussama this works"
          echo 'schedule: ${{ github.event.schedule }}'

      - name: step2
        run: |
          echo 'actor: ${{ github.actor }}'